<Window x:Class="WpfApp8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp8"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ListView Name="lvBezirke">
            <ListView.View>
                <GridView ColumnHeaderToolTip="zugeordnete Benutzer je Bezirk">
                    <GridViewColumn x:Name="colBezirk" Header="Bezirk" DisplayMemberBinding="{Binding Bezirk}"/>
                    <GridViewColumn x:Name="colBenutzer" Header="Vertreter">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox Width="300" SelectionChanged="ComboBox_SelectionChanged"
                                                    ItemsSource="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:MainWindow}}, Path=AlleBenutzer}"
                                                    SelectedValue="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext.Benutzer}" 
                                                    SelectedValuePath="Key" DisplayMemberPath="Value"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn x:Name="colVan" Header="VA" DisplayMemberBinding="{Binding Benutzer.Loginname}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
